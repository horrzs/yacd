import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'me.horrz.yacd',
    appName: 'YacdH',
    webDir: 'public',
    server: {
        androidScheme: 'https'
    }
};

export default config;
